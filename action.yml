name: "Test"
description: "Action Tests"
author: "bokuweb"
inputs:
  image-directory-path:
    description: "Path to actual images directory."
    required: true
runs:
  using: "node16"
  main: "lib/main.js"
